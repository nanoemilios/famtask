server {
    listen 80;
    listen [::]:80;
    server_name family.ossist.net;

    # Redirect HTTP → HTTPS (falls SSL vorhanden)
    # return 301 https://$host$request_uri;

    root /var/www;
    index index.html;

    # ── Statische Dateien ──
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # ── API → Python-Server weiterleiten ──
    location /api/ {
        proxy_pass         http://127.0.0.1:8081;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_read_timeout 10s;
        proxy_send_timeout 10s;

        # CORS-Header (falls nötig)
        add_header Access-Control-Allow-Origin  "*" always;
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type" always;

        if ($request_method = OPTIONS) {
            return 204;
        }
    }
}

# ── HTTPS (uncomment wenn SSL-Zertifikat vorhanden) ──
# server {
#     listen 443 ssl;
#     server_name family.ossist.net;
#     ssl_certificate     /etc/letsencrypt/live/family.ossist.net/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/family.ossist.net/privkey.pem;
#
#     root /var/www;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#         add_header Cache-Control "no-cache";
#     }
#
#     location /api/ {
#         proxy_pass         http://127.0.0.1:8081;
#         proxy_http_version 1.1;
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#     }
# }
